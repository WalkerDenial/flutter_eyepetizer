import 'package:flutter/material.dart';
import 'package:flutter_eyepetizer/pages/home/home.dart';

class WebSplash extends StatelessWidget {
  const WebSplash({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Home();
  }
}